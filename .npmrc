# Most security settings live in pnpm-workspace.yaml (pnpm v10+).
# This file only holds settings .npmrc supports authoritatively.

# Don't auto-install peer dependencies — surface missing peers explicitly
# instead of silently pulling in something we didn't review.
auto-install-peers=false
