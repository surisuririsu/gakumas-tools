export const SCENARIOS = ["hajime", "nia", "hif"];

// Canonical calculator page for each scenario (hajime's index route
// redirects, so link straight to produce-rank).
export const CALCULATOR_PATH_BY_SCENARIO = {
  hajime: "/calculator/hajime/produce-rank",
  nia: "/calculator/nia",
  hif: "/calculator/hif",
};
